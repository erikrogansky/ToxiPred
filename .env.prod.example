# Environment Configuration for Production

# Database Configuration
POSTGRES_USER=toxipred
POSTGRES_PASSWORD=change_this_in_production
POSTGRES_DB=toxipred

# Application Configuration
PUBCHEM_LOOKUP=1

# Frontend API URL (update with your domain)
API_URL=http://localhost/api

# Optional: Redis configuration (defaults are usually fine)
# CELERY_BROKER_URL=redis://toxipred-redis:6379/0
# CELERY_RESULT_BACKEND=redis://toxipred-redis:6379/0
